{
  "name": "oka",
  "private": true,
  "description": "OKA Egypt shop: the Expo app (app/) and its order service (server/).",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "dev": "node scripts/dev.mjs",
    "dev:lan": "node scripts/dev.mjs --lan",
    "app": "npm --prefix app start",
    "server": "npm --prefix server run dev",
    "test": "npm --prefix server test",
    "lint": "npm --prefix app run lint",
    "check": "npm run lint && npm test && npm --prefix app run check:bundle"
  },
  "engines": {
    "node": ">=22"
  }
}
