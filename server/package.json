{
  "name": "oka-order-service",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Credential-holding backend for the OKA app: native checkout, order status, loyalty.",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file=.env index.js",
    "dev": "node --watch --env-file=.env index.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=20"
  }
}
