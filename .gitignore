node_modules/
.expo/
dist/
web-build/
*.log

# Secrets — never commit. The Admin API token and Bosta key belong in the
# server's environment, not in this repository.
.env
.env.*
!.env.example

# macOS / editors
.DS_Store
*.tsbuildinfo

# Subscription records — real customer data generated at runtime, not
# something to ship in the repo. See server/subscriptions.js.
server/data/
